import { TableDataTransformPipe } from './table-data-transform.pipe';

describe('TableDataTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new TableDataTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
