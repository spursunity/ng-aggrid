import { GetBooleanPipe } from './get-boolean.pipe';

describe('GetBooleanPipe', () => {
  it('create an instance', () => {
    const pipe = new GetBooleanPipe();
    expect(pipe).toBeTruthy();
  });
});
